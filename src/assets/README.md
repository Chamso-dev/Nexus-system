# Assets

Static assets bundled with the bot (emoji sheets, images used in embeds,
chart templates, etc.). Kept here so the folder is versioned and referenced by a
stable path. Currently the bot renders text-based embeds and does not require
binary assets; add them here as features grow (e.g. allocation chart PNGs).
